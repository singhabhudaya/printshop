# --- Server ---
NODE_ENV=development
PORT=4000

# Comma-separate all frontend origins (no spaces)
# e.g. http://localhost:5173,https://your-frontend.com
CORS_ORIGIN=http://localhost:5173

# --- Auth ---
# Use a long, random string in real .env (do NOT commit the real value)
JWT_SECRET=change_me_now_please

# --- Database (MongoDB Atlas) ---
# Replace <db_password> (URL-encode special chars: @ -> %40, # -> %23, etc.)
# Add a DB name after the host; we use "printshop" here
MONGO_URI=mongodb+srv://theprintingmuse_db_user:<db_password>@cluster0.luq8eb3.mongodb.net/printshop?retryWrites=true&w=majority&appName=Cluster0

# --- Payments (optional for later; keep empty if Shopify handles checkout) ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# --- Email (optional, pick one provider) ---
# MAILGUN_DOMAIN=
# MAILGUN_API_KEY=
# SENDGRID_API_KEY=
# SES_ACCESS_KEY_ID=
# SES_SECRET_ACCESS_KEY=
# SES_REGION=

# --- Storage (optional) ---
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
